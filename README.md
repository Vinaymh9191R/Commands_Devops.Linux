# Commands_Devops.Linux
This is Basic commands which it used in terminal to execute some function.its help to create,make,delete,add etc. in files.
# Linux Assignment – Modules 1 to 4  
This repository contains my Linux practical assignment for Modules 1–4.  
The tasks include basic file operations, viewing system files, pattern searching, compression, downloading files, changing permissions, and working with environment variables.

---

## 📁 Repository Structure


---

## ✅ **Task List**

### **1. Creating and Renaming Files/Directories**
- `mkdir test_dir`
- `touch test_dir/example.txt`
- `mv test_dir/example.txt test_dir/renamed_example.txt`

### **2. Viewing File Contents**
- `cat /etc/passwd`
- `head -n 5 /etc/passwd`
- `tail -n 5 /etc/passwd`

### **3. Searching for Patterns**
- `grep "root" /etc/passwd`

### **4. Zipping and Unzipping Files**
- `zip -r test_dir.zip test_dir`
- `unzip test_dir.zip -d unzipped_dir`

### **5. Downloading Files Using wget**
- `wget https://example.com/sample.txt`

### **6. Changing File Permissions**
- `touch secure.txt`
- `chmod 444 secure.txt`

### **7. Working with Environment Variables**
- `export MY_VAR="Hello, Linux!"`
- `echo $MY_VAR`

---

## 📸 **Screenshots**
All task screenshots are included inside the `screenshots/` folder.  
Each screenshot corresponds to its task number.

---

## 📄 **Final Submission PDF**
The final compiled PDF containing:
- Commands  
- Explanations  
- All screenshots  
is available as:

